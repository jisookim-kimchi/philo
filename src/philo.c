#include "philo.h"

/*
	arguments 
	numger of philos, time_to_die, time_to_eat, time_to_sleep, must_eat_counts
*/

int	main(int ac, char **av)
{
	t_table	*table;
	t_philo *philo;

	if (ac != 5 && ac != 6)
		return (-1);
}
